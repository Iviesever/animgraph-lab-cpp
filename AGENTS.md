# AnimGraphLab agent instructions

- Read `.agents/rules/project.md` and the active `tasks/*/` contract before work.
- Use test-first RED/GREEN evidence for production behavior.
- Keep every generated artifact, trace, screenshot, and report inside this repo.
- MQB is the primary Windows/MSVC developer entry; CMake/CTest is the portable path.
- Do not modify sibling repositories or system configuration.
- Do not merge the delivery PR, create tags, publish releases, or upload binaries.
